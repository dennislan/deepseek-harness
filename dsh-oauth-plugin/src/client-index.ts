/**
 * Client entry point for dsh-oauth-plugin.
 * @module dsh-oauth-plugin/client
 */

export { apply, inject, createAuthStore } from './client.ts'
/** @typedef {import('./client.ts').AuthState} AuthState */
/** @typedef {import('./client.ts').AuthStore} AuthStore */
